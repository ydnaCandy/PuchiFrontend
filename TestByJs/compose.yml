version: "3.9"

services:
  web:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "20080:80"
    volumes:
      - ./apps:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always